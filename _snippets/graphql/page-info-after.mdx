```graphql filename="Second page"
query getCustomers($cursor: String!) {
  customers(after: $cursor, first: 50) {
    edges {
      node {
        fullName
      }
    }
    totalCount
    pageInfo {
      hasPreviousPage
      hasNextPage
      startCursor
      endCursor
    }
  }
}
```
